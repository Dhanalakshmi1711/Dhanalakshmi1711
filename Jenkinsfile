pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo("::::::::::::::::::::::::")
                sh("pip")
                sh("python3 app.py")
                
            }
        }
    }
}
